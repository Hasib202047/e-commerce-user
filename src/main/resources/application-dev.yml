eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    refresh:
      enable: true
    service-url:
      defaultZone: http://localhost:8000/eureka
  instance:
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 30

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/e_commerce_user
    username: postgres
    password: h@sib2211
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  data:
    redis:
      port: 6379
      host: localhost
